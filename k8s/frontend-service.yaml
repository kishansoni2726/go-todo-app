apiVersion: v1
kind: Service
metadata:
  name: todo-ui
spec:
  type: NodePort
  selector:
    app: todo-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000  # Choose a port between 30000–32767